const settingPageReducer = {
  settingPageResult: (
    state = { },
    action
  ) => {
    switch (action.type) {
      default:
        return state;
    }
  },
};

export default settingPageReducer;
